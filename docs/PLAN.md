- 16:40 atlas 0.2.5 to 0.2.9 shipped, blueprint 0.4.0 to 0.8.0. The real cause of the photographed broken highway was in the preview run tracer: it walked forward only, so a 3 km city drew 31 decks where there are 15 chains, 16 spurious ends each ramping down mid-city beside a building. Runs now grow both ways and ramp only at a terminus within 30 m of the city edge (15 decks on 3 km, 1 on the default). Stations publish platform, box and shafts (subway -12 to -7, train 0 to 3), entrances moved onto the sidewalk beside the platform (passage 0 to 2.6 m, was up to 110 m). The kerb is a fifth ground surface at 0.15 m, unbroken through junction returns (18,647 samples, none uncovered). Districts are rectangles on the city grid with meta.gridAngle published; irregularity is the only thing that leaves the grid (0.3 to 4.4 degrees at 0.35). Two bugs found on the way: the alley pass rebuilt the graph without re-checking planarity, and the block interior-area invariant was loose. Relayed: station volumes and the kerb to connections (resumed) and the engine; the street furniture proposal waits on three answers from the engine; the rung-300:p3 massing case ruled to exterior (its box must keep a plate hosting interior's core rectangle, walkup 9.9 x 8.5, compact 10.9 x 12.5).
- 16:55 materials 0.9.5 to 0.9.10 shipped: ac-unit condenser kind (the user's "no acs", exterior hangs facade units on it), frame steel flat and held by a test, the steel kinds off the grain ramp with the wet and over-bright audit closed across the library (roughness sd at most 0.017, nothing below 0.46), roof and floor-slab leading with a solid plain deck so a partial tile shows no cut joint (the user's own note on the screenshot), the rebrand empty-list ruling, and the shipped index pruned of 84 brand variants that pointed at 514 MB of never-committed local output (a fresh clone could not resolve ad-screen at all). Integration note for the rebuild: a world now rebrands into its own copy of the theme folder, so run materials' rebrand against that world's businesses.json after assembling, and expect brand keys to be unresolved until then.
- 17:00 atlas 0.2.11 shipped, blueprint 0.10.0. Highway deck, ramp and support arithmetic moved from the preview into `streets.highwayStructures`: every edge is covered once, supports are 2 x 2 m on the 1 mm grid at 30 m or less, and parcels reserve the full deck with 1 m construction clearance on each side. The generator fails closed on any deck or support overlap with a building footprint; the preview reads this data directly.
- 17:10 atlas 0.2.12 shipped, blueprint 0.11.0. The at-grade train route used to be laid after zoning and crossed default-city building p24 by 304.03 m2 (and 3 km p10 by 224.58 m2). Train track and platforms now reserve their right-of-way plus 1 m before parcels are cut; rail lines publish width; train platforms stay outside highway decks; subway entrances select clear sidewalk positions. Highway supports move across the deck or shift their pitch backward around track and entrance shafts. Placement advances monotonically or fails closed. Clearance tests cover four seeds at 1, 2 and 3 km.
- 17:20 atlas 0.2.13 shipped. The default flat preview defers hidden 3D mesh construction until 3D is selected. Mixed station geometries are normalized before material merging, removing the Three.js compatibility error that discarded station meshes.
- 17:35 atlas 0.2.14 shipped. The tensor field no longer rotates streets with seeded value noise: default interior centerlines measure within 0.5 degrees of the shared city axes; only the computed boundary field and the radial downtown selected at irregularity 0.4 can curve them. Sub-0.5 m boolean edges are removed before curb returns read their direction, and curb fragments below that run become sidewalk instead of a sliver. Samples regenerated: 147 / 80 / 21 parcels.
- 17:40 atlas 0.2.15 shipped. Nested parameters now validate as a closed runtime shape, including browser imports. Malformed ranges and maps, unknown enum keys, non-boolean toggles and non-finite numbers return `E_INVALID_PARAMS` rather than passing through or throwing a raw TypeError.
- 17:50 atlas 0.2.16 shipped. The 3D preview uses one capped envelope prism per building with facade lines at the published floor elevations. It no longer triangulates and renders a closed solid per floor, whose internal caps were hidden but dominated the zoomed-out city.
