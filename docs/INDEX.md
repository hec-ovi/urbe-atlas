# Box map

- atlas (root): deterministic 2D city blueprint generator. CONTRACT.md, schemas in schema/, generator in src/.
- src/ui: preview box (canvas map view with parcel picking, params panel with parameter import/export, progress cover, notifications, parcel link template, legend, layer toggles). src/ui/CONTRACT.md. Depends on the root contract only.
