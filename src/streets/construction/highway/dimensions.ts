/** Highway construction dimensions shared by the blueprint and preview. */
export const HIGHWAY_DECK = {
  thickness: 1,
  rampLength: 60,
  supportPitch: 30,
  supportSize: 2,
  buildingClearance: 1,
} as const;
