{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic 2D city map generator: seed plus params to a typed city blueprint",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "preview": "vite",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
